@model MusicStoreInfo.Domain.Entities.ShoppingCart

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Корзина</h1>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>
               Название
            </th>
            <th>
                Цена
            </th>
            <th>
                Магазин
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Products)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Album.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Store.Name)
                </td>
                <td>
                    <a asp-action="PayForTheProduct" asp-route-storeId="@item.StoreId" asp-route-albumId="@item.AlbumId" class="btn btn-primary btn-sm">Оплатить</a>
                    <a asp-action="DeleteFromShoppingCart" asp-route-storeId="@item.StoreId" asp-route-albumId="@item.AlbumId" class="btn btn-danger btn-sm">Удалить из корзины</a>
                </td>
            </tr>
        }
    </tbody>
</table>
